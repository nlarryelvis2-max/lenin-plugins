{
  "name": "@lenin-ecosystem/client",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Public Lenin Client and provider-neutral Lenin Core projection.",
  "exports": {
    "./platform-core": "./lenin-core/platform-core.js"
  },
  "files": [
    "lenin-core/platform-core.js",
    "lenin-core/skills/*/SKILL.md"
  ],
  "scripts": {
    "test": "node --test test/*.test.js && python3 -m unittest discover -s client/scripts -p 'test_*.py' -v && python3 -m unittest discover -s lenin-uplink/scripts -p 'test_*.py' -v"
  }
}
