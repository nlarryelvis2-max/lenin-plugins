{
  "name": "@lenin-ecosystem/client",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "description": "Public Lenin Client, Owner MCP and provider-neutral Lenin Core projection.",
  "exports": {
    "./platform-core": "./lenin-core/platform-core.js"
  },
  "files": [
    "lenin-core/platform-core.js",
    "lenin-core/skills/*/SKILL.md",
    "owner/scripts/owner_client.py",
    "owner/scripts/owner_mcp.py"
  ],
  "scripts": {
    "test": "node --test test/*.test.js && python3 -m unittest discover -s client/scripts -p 'test_*.py' -v && python3 -m unittest discover -s lenin-uplink/scripts -p 'test_*.py' -v && python3 -m unittest discover -s owner/scripts -p 'test_*.py' -v"
  }
}
